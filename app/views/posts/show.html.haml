= image_tag @post.image.url(:small)
%h1= @post.title
%p= @post.link
%p= @post.description
%p= @post.user.name

%button= link_to "Edit", edit_post_path(@post)
%button= link_to "Destroy", post_path(@post), method: :delete, data: { confirm: "Are you sure?" }
%button= link_to "Home", root_path